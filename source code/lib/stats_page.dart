import 'package:flutter/material.dart';

class stats_page extends StatelessWidget {
  const stats_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello')
    );
  }
}